import styled from 'styled-components'

const Form = styled.form`
  margin-bottom: 2em;
  & .MuiFormControl-root {
    width: 100%;
  }
  & .MuiButtonBase-root {
    position: relative;
    top: 1em;
  }
`
export default Form
